// TODO: [Dev A] Teams list + group filter + standings; team cards using shared design system.
export default function Teams() {
  return (
    <section className="page">
      <h1>Teams</h1>
      <p className="muted">Placeholder — owned by Dev A (Teams &amp; Standings).</p>
    </section>
  );
}
